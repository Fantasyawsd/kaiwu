# 算法总表

本文件是当前仓库已实现算法的统一清单，记录算法状态、训练得分与文档路径。

职责边界：

- 本文件由 AI 维护，用于反映仓库内已经实现并落库的算法状态。
- 本文件只记录已实现算法；尚未实现的候选方向不写在这里。
- 未实现的外部调研候选方案统一记录在 `GLOBAL_DOCS/算法调研.md`。
- 当前正在实现但尚未完成的工作统一记录在 `DEV_MEMORY/*`。

- 若某个算法经过正式训练确认优于旧基线，并被选定为新基线，必须在本文件中切换“当前基线”标记。
- 当前算法实现源码目录固定为 `agent_ppo/`；`agent_diy/` 仅作为模板，不作为正式算法实现目录。
- 若需定位某个历史算法实现，优先通过“完整算法 commit 中标注的算法完整名 + git 回退”定位，而不是依赖源码地址字段。

## 当前算法索引

| 角色 | 算法名 | 算法完整名 | 状态 | 文档 |
| --- | --- | --- | --- | --- |
| 当前基线 | `ppo` | `agent_ppo_20260409_1733` | 已实现 | `GLOBAL_DOCS/算法文档/agent_ppo_20260409_1733.md` |

## 字段说明

| 字段    | 说明                                                                |
| ----- | ----------------------------------------------------------------- |
| 算法名   | 算法标识（对应 `conf/algo_conf_gorge_chase.toml` 中的 key）                 |
| 算法完整名 | 当前这一次完整算法实现的唯一名字，建议与正式算法文档文件名去掉 `.md` 后保持一致；完整算法 commit 必须显式包含该名字 |
| 算法描述  | 简要说明算法思路与特点                                                       |
| 训练得分  | 可确认的平台训练得分；无正式结果时写“暂无正式训练得分”                                      |
| 算法文档  | 正式算法文档路径（使用 算法名_时间.md 命名）                                         |
| 备注    | 当前状态说明、已知限制、下一步                                                   |

---

## 当前算法清单

### agent_ppo（PPO 实现）

| 字段    | 值                                                                                                                    |
| ----- | -------------------------------------------------------------------------------------------------------------------- |
| 算法名   | `ppo`                                                                                                                |
| 算法完整名 | `agent_ppo_20260409_1733`                                                                                            |
| 算法描述  | Proximal Policy Optimization 基线。结构化特征编码（40维）+ Actor-Critic 双头 + PPO clip loss + GAE + legal action masking + 轻量探索奖励。 |
| 训练得分  | 暂无正式训练得分（已完成 smoke 验证，smoke test 通过）                                                                                 |
| 算法文档  | `GLOBAL_DOCS/算法文档/agent_ppo_20260409_1733.md`                                                                        |
| 备注    | 当前仓库主训练对象。所有正式算法迭代代码都在 `agent_ppo/` 中实现；动作空间只用 8 维移动动作（环境原生 16 维），未接入闪现。奖励分两层：preprocessor 即时奖励 + workflow 终局奖励。     |
